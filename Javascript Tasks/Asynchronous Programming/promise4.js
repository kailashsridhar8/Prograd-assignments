const posts=[
    {title:"First", body:"First Content"},
    {title:"Second", body:"Second Content"}
];

function getPosts(){
    setTimeout(() =>{
        posts.forEach((post) =>{ 
        
        }) 
        },1000);
}
